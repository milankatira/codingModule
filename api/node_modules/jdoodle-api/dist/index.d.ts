export interface Language {
    languageCode: string;
    versionIndex: number;
}
export interface JDoodleResponse {
    output: string;
    statusCode: number;
    memory: number;
    cpuTime: number;
    error?: string;
}
declare const run: (language: Language, script: string) => Promise<JDoodleResponse>;
export default run;
